# Chapter 20 - Close the project

## 20.01 - Look Back At The Finished App

The build is finished enough to prove. This step turns the project into something you can demo, explain, and improve without reopening every decision.

The weak approach is to keep adding features because another feature feels easier than calling v1 done. The professional approach is to freeze the core, rehearse the story, write the trade-offs, and choose a small backlog.

✅ Do connect every claim in your demo to a route, table, function, or deployed behavior. ❌ Don't rely on screenshots when you can show the working app.

Write the result in `learning-log/20-closing.md`.

---

## 20.02 - Prepare The Demo

The build is finished enough to prove. This step turns the project into something you can demo, explain, and improve without reopening every decision.

The weak approach is to keep adding features because another feature feels easier than calling v1 done. The professional approach is to freeze the core, rehearse the story, write the trade-offs, and choose a small backlog.

✅ Do connect every claim in your demo to a route, table, function, or deployed behavior. ❌ Don't rely on screenshots when you can show the working app.

Write the result in `learning-log/20-closing.md`.

---

## 20.03 - Explain The System

The build is finished enough to prove. This step turns the project into something you can demo, explain, and improve without reopening every decision.

The weak approach is to keep adding features because another feature feels easier than calling v1 done. The professional approach is to freeze the core, rehearse the story, write the trade-offs, and choose a small backlog.

✅ Do connect every claim in your demo to a route, table, function, or deployed behavior. ❌ Don't rely on screenshots when you can show the working app.

Write the result in `learning-log/20-closing.md`.

---

## 20.04 - Answer Review Questions

The build is finished enough to prove. This step turns the project into something you can demo, explain, and improve without reopening every decision.

The weak approach is to keep adding features because another feature feels easier than calling v1 done. The professional approach is to freeze the core, rehearse the story, write the trade-offs, and choose a small backlog.

✅ Do connect every claim in your demo to a route, table, function, or deployed behavior. ❌ Don't rely on screenshots when you can show the working app.

Write the result in `learning-log/20-closing.md`.

---

## 20.05 - Choose Future Improvements

The build is finished enough to prove. This step turns the project into something you can demo, explain, and improve without reopening every decision.

The weak approach is to keep adding features because another feature feels easier than calling v1 done. The professional approach is to freeze the core, rehearse the story, write the trade-offs, and choose a small backlog.

✅ Do connect every claim in your demo to a route, table, function, or deployed behavior. ❌ Don't rely on screenshots when you can show the working app.

Write the final v2 ideas in `docs/future-backlog.md`, then summarize the trade-off in `learning-log/20-closing.md`.

---

## 20.06 - Write The Final Learning Log

The build is finished enough to prove. This step turns the project into something you can demo, explain, and improve without reopening every decision.

The weak approach is to keep adding features because another feature feels easier than calling v1 done. The professional approach is to freeze the core, rehearse the story, write the trade-offs, and choose a small backlog.

✅ Do connect every claim in your demo to a route, table, function, or deployed behavior. ❌ Don't rely on screenshots when you can show the working app.

Write the result in `learning-log/20-closing.md`.

---

## 20.07 - Final Definition Of Done

This checklist is the gate for Chapter 20. Do not move on until every item is true in your own project.

- [ ] Final demo and next-step plan exists in the repo.
- [ ] The main route, screen, table, or function for `project closure` can be opened or inspected.
- [ ] The happy path has been tested.
- [ ] A failure, empty, or validation state has been tested.
- [ ] A privacy or secret-safety check has been tested where relevant.
- [ ] `learning-log/20-closing.md` explains the decision and the proof.
- [ ] `docs/future-backlog.md` lists only small v2 improvements that do not reopen v1.

All boxes ticked? Then continue to the next chapter. If not, that is where today's work is.
